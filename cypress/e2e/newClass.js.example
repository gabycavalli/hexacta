// let arr = [6, 1, 2, 7, 4, 8, 3, 1, 8, 9, 2, 3, 9];
// let arrLength = arr.length;

// for (let i = 0; i < arrLength - 1; i++) {
//   for (let j = 0; j < arrLength - i - 1; j++) {
//     if (arr[j] < arr[j + 1]) {
//       // Intercambiar elementos si están en el orden incorrecto
//       let temp = arr[j];
//       arr[j] = arr[j + 1];
//       arr[j + 1] = temp;
//     }
//   }
// }

// console.log(arr);

// let arr = [6, 1, 2, 7, 4, 8, 3, 1, 8, 9, 2, 3, 9];
// let arr2 = arr.slice(); // Copiar el arreglo original
// arr2.sort(function (a, b) {
//   return a - b; // Ordenar en orden ascendente
// });
// console.log(arr2);

// let letras = ["c", "a", "d", "b"];
// let n = letras.length;
// let temp;

// for (let i = 0; i < n - 1; i++) {
//   for (let j = 0; j < n - i - 1; j++) {
//     if (letras[j] > letras[j + 1]) {
//       // Intercambiar elementos si están en el orden incorrecto
//       temp = letras[j];
//       letras[j] = letras[j + 1];
//       letras[j + 1] = temp;
//     }
//   }
// }

// console.log(letras); // Resultado: ['a', 'b', 'c', 'd']

// let letras = ["c", "a", "d", "b"];
// let n = letras.length;
// let temp;

// for (let i = 0; i < n - 1; i++) {
//   for (let j = 0; j < n - i - 1; j++) {
//     if (letras[j] < letras[j + 1]) {
//       // Intercambiar elementos si están en el orden incorrecto (cambio de '<' a '>')
//       temp = letras[j];
//       letras[j] = letras[j + 1];
//       letras[j + 1] = temp;
//     }
//   }
// }

// console.log(letras); // Resultado: ['d', 'c', 'b', 'a']

// let letras = ['c', 'a', 'd', 'b'];
// letras.sort(); // Ordenar en orden ascendente
// console.log(letras); // Resultado: ['a', 'b', 'c', 'd']

// let letras = ['c', 'a', 'd', 'b'];
// letras.sort(); // Ordenar en orden ascendente primero
// letras.reverse(); // Luego, invertir el orden para obtener descendente
// console.log(letras); // Resultado: ['d', 'c', 'b', 'a']

// function esVocal(letra) {
//   // Convierte la letra a minúscula para manejar tanto mayúsculas como minúsculas
//   letra = letra.toLowerCase();
//   return 'aeiou'.includes(letra); // Verifica si la letra está en el conjunto de vocales
// }

// let letras = ['a', 'b', 'c', 'd', 'e', 'f', 'g'];
// let vocales = [];

// for (let i = 0; i < letras.length; i++) {
//   if (esVocal(letras[i])) {
//     vocales.push(letras[i]);
//   }
// }

// console.log(vocales); // Resultado: ['a', 'e']

// let letras = ['a', 'b', 'c', 'd', 'e', 'f', 'g'];

// // Letra o letras que deseas eliminar
// let letraAEliminar = 'c';
// // Si deseas eliminar un grupo de letras, puedes usar una expresión regular, por ejemplo:
// // let letrasAEliminar = /[aeiou]/;

// let letrasFiltradas = letras.filter(letra => letra !== letraAEliminar);

// console.log(letrasFiltradas); // Resultado: ['a', 'b', 'd', 'e', 'f', 'g']

// pares o impares

// let numeros = [1, 2, 3, 4, 5, 6, 7, 8, 9];
// let numerosPares = numeros.filter(numero => numero % 2 === 0);
// let numerosImpares = numeros.filter(numero => numero % 2 !== 0);

// console.log('Números Pares:', numerosPares); // Resultado: [2, 4, 6, 8]
// console.log('Números Impares:', numerosImpares); // Resultado: [1, 3, 5, 7, 9]

// Positivos o Negativos

// let numeros = [-3, -2, -1, 0, 1, 2, 3];
// let numerosPositivos = numeros.filter(numero => numero > 0);
// let numerosNegativos = numeros.filter(numero => numero < 0);

// console.log('Números Positivos:', numerosPositivos); // Resultado: [1, 2, 3]
// console.log('Números Negativos:', numerosNegativos); // Resultado: [-3, -2, -1]

// Primos

// function esPrimo(numero) {
//   if (numero <= 1) return false;
//   if (numero <= 3) return true;
//   if (numero % 2 === 0 || numero % 3 === 0) return false;
//   let i = 5;
//   while (i * i <= numero) {
//     if (numero % i === 0 || numero % (i + 2) === 0) return false;
//     i += 6;
//   }
//   return true;
// }

// let numeros = [1, 2, 3, 4, 5, 6, 7, 8, 9];
// let numerosPrimos = numeros.filter(numero => esPrimo(numero));

// console.log('Números Primos:', numerosPrimos); // Resultado: [2, 3, 5, 7]
